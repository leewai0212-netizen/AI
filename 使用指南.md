# 抖音评论爬虫使用指南

## 📚 目录

1. [快速开始](#快速开始)
2. [安装步骤](#安装步骤)
3. [三种使用方式](#三种使用方式)
4. [常见问题解决](#常见问题解决)
5. [进阶使用](#进阶使用)

---

## 🚀 快速开始

### 最简单的方式（推荐新手）

1. **安装依赖**
   ```bash
   pip install -r requirements.txt
   ```

2. **运行交互式脚本**
   ```bash
   python3 quick_start.py
   ```

3. **按提示输入**
   - 粘贴抖音视频链接
   - 输入要爬取的评论数量
   - 选择保存格式

就这么简单！程序会自动完成剩余工作。

---

## 📦 安装步骤

### 方法1: 自动安装（Linux/Mac）

```bash
bash install.sh
```

### 方法2: 手动安装

1. **确保已安装Python 3.7+**
   ```bash
   python3 --version
   ```

2. **安装Chrome浏览器**
   
   - Windows/Mac: 从 https://www.google.com/chrome/ 下载
   - Linux Ubuntu/Debian:
     ```bash
     wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
     sudo dpkg -i google-chrome-stable_current_amd64.deb
     sudo apt-get install -f
     ```

3. **安装Python依赖**
   ```bash
   pip3 install -r requirements.txt
   ```

---

## 🎯 三种使用方式

### 方式1: 交互式脚本（最简单）

```bash
python3 quick_start.py
```

适合：新手、快速使用、单次爬取

### 方式2: 运行示例代码

```bash
python3 example.py
```

示例代码包含4个使用场景：
- 基础使用
- 使用配置文件
- 批量爬取多个视频
- 数据分析

修改 `example.py` 中的视频链接即可使用。

### 方式3: 编写自定义代码

创建自己的Python脚本：

```python
from douyin_comment_scraper import DouyinCommentScraper

# 创建爬虫
scraper = DouyinCommentScraper(headless=False)

try:
    # 爬取评论
    comments = scraper.scrape_comments(
        video_url="你的视频链接",
        max_comments=100,
        scroll_times=10
    )
    
    # 保存数据
    scraper.save_to_json('comments.json')
    scraper.save_to_csv('comments.csv')
    
finally:
    scraper.close()
```

---

## 🔍 如何获取抖音视频链接

### 电脑端：

1. 打开抖音网页版: https://www.douyin.com
2. 搜索或浏览找到目标视频
3. 点击视频进入详情页
4. 复制浏览器地址栏的URL

### 手机端：

1. 打开抖音APP
2. 找到目标视频
3. 点击右下角"分享"按钮
4. 选择"复制链接"
5. 将链接发送到电脑

**支持的链接格式：**
- `https://www.douyin.com/video/7234567890123456789`
- `https://v.douyin.com/短链接/`（会自动跳转）

---

## ❓ 常见问题解决

### 问题1: 提示找不到Chrome或ChromeDriver

**解决方案A: 安装Chrome浏览器**
```bash
# Ubuntu/Debian
wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
sudo dpkg -i google-chrome-stable_current_amd64.deb
sudo apt-get install -f
```

**解决方案B: 使用webdriver-manager自动管理**

已在 `requirements.txt` 中包含，会自动下载匹配的ChromeDriver。

### 问题2: 未找到评论或评论数量很少

**可能原因：**
1. 视频本身评论就不多
2. 需要登录才能看评论
3. 页面加载时间不够

**解决方法：**

1. **增加滚动次数和等待时间**
   ```python
   comments = scraper.scrape_comments(
       video_url=url,
       max_comments=200,
       scroll_times=20  # 增加到20次
   )
   ```

2. **使用非无头模式查看实际情况**
   ```python
   scraper = DouyinCommentScraper(headless=False)
   ```
   这样可以看到浏览器实际操作过程。

3. **手动登录**
   
   程序打开浏览器后，暂停让用户手动登录：
   ```python
   scraper._init_driver()
   scraper.driver.get("https://www.douyin.com")
   input("请在浏览器中登录后按回车继续...")
   ```

### 问题3: 遇到滑动验证码

**解决方法：**

使用非无头模式，程序会暂停等待你手动完成验证：

```python
scraper = DouyinCommentScraper(headless=False)
```

看到验证码时，手动滑动完成验证，程序会自动继续。

### 问题4: 提示ModuleNotFoundError

**原因：** 依赖包未安装

**解决：**
```bash
pip3 install -r requirements.txt
```

如果还是有问题，逐个安装：
```bash
pip3 install selenium pandas openpyxl webdriver-manager
```

### 问题5: 页面结构变化，无法提取评论

**原因：** 抖音更新了页面结构

**解决方法：**

1. 查看保存的页面源代码：
   ```bash
   # 程序会自动保存到 page_source.html
   open page_source.html  # Mac
   xdg-open page_source.html  # Linux
   ```

2. 分析新的CSS选择器，修改 `douyin_comment_scraper.py` 中的选择器列表

---

## 🎓 进阶使用

### 1. 批量爬取多个视频

```python
video_urls = [
    "https://www.douyin.com/video/1111111111111111111",
    "https://www.douyin.com/video/2222222222222222222",
    "https://www.douyin.com/video/3333333333333333333",
]

scraper = DouyinCommentScraper(headless=False)

try:
    for idx, url in enumerate(video_urls, 1):
        print(f"\n处理第 {idx}/{len(video_urls)} 个视频...")
        
        comments = scraper.scrape_comments(url, max_comments=50)
        
        if comments:
            scraper.save_to_json(f'video_{idx}_comments.json')
            scraper.save_to_csv(f'video_{idx}_comments.csv')
        
        time.sleep(5)  # 每个视频之间暂停5秒
finally:
    scraper.close()
```

### 2. 数据分析示例

```python
import pandas as pd

# 读取CSV文件
df = pd.read_csv('douyin_comments.csv')

# 基础统计
print("评论数量:", len(df))
print("总点赞数:", df['likes'].sum())
print("平均点赞数:", df['likes'].mean())
print("最高点赞数:", df['likes'].max())

# 找出点赞最多的前10条评论
top_10 = df.nlargest(10, 'likes')
print("\n点赞最多的10条评论:")
print(top_10[['username', 'content', 'likes']])

# 词云分析（需要安装wordcloud）
# from wordcloud import WordCloud
# import matplotlib.pyplot as plt
# 
# text = ' '.join(df['content'].tolist())
# wordcloud = WordCloud(font_path='simhei.ttf').generate(text)
# plt.imshow(wordcloud)
# plt.show()
```

### 3. 定时任务

使用crontab设置定时爬取：

```bash
# 编辑crontab
crontab -e

# 每天早上9点爬取
0 9 * * * cd /path/to/project && python3 quick_start.py
```

### 4. 修改默认配置

编辑 `config.py` 文件：

```python
# 浏览器配置
HEADLESS = True  # 改为True使用无头模式

# 爬虫配置
MAX_COMMENTS = 200  # 增加默认爬取数量
SCROLL_TIMES = 20   # 增加滚动次数

# 输出配置
OUTPUT_EXCEL = True  # 启用Excel输出
```

### 5. 使用代理（可选）

如果需要使用代理：

```python
chrome_options = Options()
chrome_options.add_argument('--proxy-server=http://your-proxy:port')
```

---

## 📊 输出数据说明

### JSON格式
```json
[
  {
    "index": 1,
    "username": "用户昵称",
    "content": "评论内容",
    "likes": 123,
    "time": "2天前"
  }
]
```

### CSV格式
可以用Excel、Google Sheets等工具打开，适合数据分析。

### Excel格式
使用 `openpyxl` 生成，格式美观，适合报告。

---

## 📝 使用建议

1. **首次使用建议使用非无头模式** (`headless=False`)，观察程序运行过程
2. **从小数量开始测试**，确保能正常工作后再增加数量
3. **注意频率**，不要过于频繁爬取，避免被限制
4. **保存原始数据**，JSON格式保留了完整信息
5. **定期备份数据**，避免数据丢失

---

## ⚖️ 法律声明

- 本工具仅供学习和研究使用
- 请遵守抖音平台的使用条款和robots.txt
- 不要用于商业用途
- 尊重他人隐私和数据权利
- 合理控制爬取频率

---

## 🆘 获取帮助

如遇到问题：

1. 查看本文档的"常见问题解决"部分
2. 查看 `README.md` 了解更多技术细节
3. 查看 `page_source.html` 分析页面结构
4. 提交Issue到项目仓库

---

**祝使用愉快！** 🎉
